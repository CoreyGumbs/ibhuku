from django.conf.urls import include, url
from django.core.urlresolvers import reverse_lazy


urlpatterns = [
    url(r'^(?P<pk>\d+)$', , name='dashboard'),
    # url(r'^$', , name='update-profile'),
]
